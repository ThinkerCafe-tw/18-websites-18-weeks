# 04-build

本資料夾是 CTS 教材的 build 與預覽入口。

## Active command

```bash
python3 04-build/scripts/build-book.py
```

輸入來源：

- `01-current/`

輸出位置：

- `04-build/previews/book-preview.html`

## Legacy

舊版 builder、fact-check 與 19 講驗收頁已移入：

- `04-build/scripts/legacy/`
- `04-build/previews/legacy/`

這些檔案只作為歷史參考，不是目前正式稿 pipeline。
